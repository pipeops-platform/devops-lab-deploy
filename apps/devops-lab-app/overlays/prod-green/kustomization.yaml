apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

namespace: devops-lab-app-prod-green

images:
  - name: pipeops/devops-lab-app
    newTag: "0.3.0"

patches:
  - path: patch-deployment.yaml
  - path: patch-ingress.yaml
  - path: patch-disable-local-postgres.yaml
